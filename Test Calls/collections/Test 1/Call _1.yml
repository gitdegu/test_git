info:
  name: Call _1
  type: http
  seq: 1

http:
  method: GET
  url: https:///www.google.com
  auth: inherit

runtime:
  scripts:
    - type: after-response
      code: |-
        test ("Checkioong status", function(){
          expect(res.getStatus()).to.equal(200);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
